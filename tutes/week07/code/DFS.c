// Implementation of DFS Algorithm (non-recursive)

#include <stdio.h>
#include <stdlib.h>
#include "DFS.h"
#include "GraphMatrix.h"

void depthFirst(Graph g, Vertex V)
{
    int *visited = calloc(g->nV, sizeof(int));
    Stack s = newStack();
    StackPush(s,v);
    while (!StackIsEmpty)

}
